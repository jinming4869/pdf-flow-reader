# 更新日志

本项目遵循轻量、可读、可维护的更新记录方式。版本号发布前的改动先归入 `Unreleased`。

## Unreleased

暂无。

## v4.0.0 - 2026-08-01

这一版以“希声”为关键词，把「夜晚的书斋」从节奏阅读器推进为本地阅读陪伴：语音不再是一套覆盖所有速度的按钮，而会随六档阅读节奏改变自己的出现方式。

### 新增

- 新增英文、中文、日文全本地 TTS。中文默认 `zf_xiaobei`，日文默认 `jf_alpha`，英文默认 `af_heart`。
- 新增六档独立朗读策略：
  - “雪国朦胧”逐字随读、跨页预加载，不读页眉页脚；
  - “美学散步”用 1.5–2.5 倍动态语速跟随页面，过滤页眉页脚、脚注、注释和完整括号内容；
  - “长日留痕”显示阅读线，按 `R` 或乐符动作只读阅读线下最近一句；
  - “流觞曲水”在前段段首预备、段尾触发下一自然段段首句；
  - “强风吹拂”和“万物繁盛”只在开启顶部点读后，点击 PDF 句子“点哪里读哪里”。
- 新增原生文字与 OCR 的统一朗读门槛：原生正文够好时直接开放希声，否则自动调度附近页 OCR。
- 新增中英、日英混排路由：拉丁字母片段使用英文 worker，中日文片段使用 CJK worker，并在本地拼接为 PCM16 WAV。
- 新增自包含 CJK worker、英文离线模型和模型清单；桌面包首次朗读不再下载模型。

### 修复

- 修复 OCR 防抖任务可能被持续刷新而永久饿死的问题。
- 修复跳页只更新前端状态、没有真正取消旧朗读的问题。
- 修复取消请求没有终止正在运行的模型推理的问题；超时、跳页和主动取消会杀死旧 worker，后续请求再安全重建。
- 修复 `R` 快捷键绕过正常文字质量、页面状态和档位门槛的问题。
- 将 CPU 密集的 TTS 推理移出本地 HTTP 主进程，避免朗读时阻塞心跳、Range 读取和界面响应。
- 修复纯英文 PDF 仍显示中文“字/分”的语言判断问题，并明确 OCR 同时服务于文字可用性和速度估算精度。

### 发布工程

- CJK 正式运行时不再分发 `phonemizer-fork`、eSpeak 或 `kokoro-onnx`；改用固定 Kokoro v1.0 ONNX 协议的小型推理层。
- 用 PyInstaller onedir 携带 Python 3.12、ONNX Runtime、Misaki、pyopenjtalk 和日文字典。
- GitHub Actions 在 macOS 与 Windows 各自构建原生 worker，自动下载并校验固定哈希的模型。
- Release workflow 只有在双平台构建均成功后才附加 macOS zip、Windows portable exe 与统一 `SHA256SUMS.txt`。
- 回归测试扩展到 289 项 Node 测试与 5 项 Python worker 单测。

### 范围说明

- 本版仍是未签名个人应用，尚未做 macOS 公证或 Windows 代码签名。
- 本版不加入云端 TTS、自动更新、音乐伴奏或大范围切档配色变化。
- 本版正式支持 macOS Apple Silicon 与 Windows x64；Intel macOS 和 Linux 暂不提供构建资产。

## v3.0.0 - 2026-07-24

这一版以“回声”为设计关键词，将「夜晚的书斋」从自动滚动 PDF 阅读器升级为本地节奏阅读器。重点是阅读记忆、节奏仪表、视觉背景、切档音效和未来 TTS 架构预留。

### 新增

- 新增阅读回声首页：显示“最近在读什么书？”，记录最近阅读、上次位置、上次速度和节奏回声。
- 新增本地阅读记录数据层：`DocumentRecord`、`RhythmRecord`、`UserPreferences`，支持清除本机阅读回声。
- 新增节奏仪表盘基础版：字/分、词/分改为范围估算，并将“以当前速度，估算还需阅读约 X 分钟”移动到顶部状态区。
- 新增六档视觉节奏背景：每个速度档位拥有不同色彩、图案和自绘 SVG 背景元素。
- 新增背景朦胧 / 清晰模式：点击 PDF 外围背景即可切换；设置保存在本机。
- 新增切档音效：朦胧背景使用轻合成器提示音，清晰背景使用本地 m4a 切档音效。
- 新增 10 个清晰模式切档音效：`up-1` 到 `up-5`、`down-1` 到 `down-5`。
- 新增未来 TTS 架构预留：`TextSegment`、`ReadingClock` 与 `docs/TTS_PROVIDER.md`，但本版不实现朗读功能。

### 改进

- 顶部栏显示剩余阅读时间，底部控制栏只保留速度、密度、滑杆和页码，降低阅读时的信息拥挤。
- 切档音效会聚合连续滑动手势，并按顺序播放，避免多个音效重叠。
- 首页文案收紧为“让 PDF 安静地流过眼前”，减少说明文字对入口页的挤压。
- 背景图案只出现在 PDF 纸页外侧，不覆盖或染色 PDF 正文。
- 清晰模式下音频文件缺失或加载失败时自动回退到合成器提示音。

### 工程化

- 新增模块：
  - `storage.mjs`
  - `reading-rhythm.mjs`
  - `reading-clock.mjs`
  - `text-segment.mjs`
  - `sound-engine.mjs`
- 新增测试覆盖阅读记忆、节奏快照、阅读时钟、文本片段、音效边界映射和静态资源服务。
- 回归测试扩展到 45 项通过。

### 范围说明

- 本版不实现 Zotero 集成。
- 本版不实现 TTS 朗读、模型下载、API 调用或文本上传。
- 本版不加入批注、PDF 编辑、云同步或文献管理功能。

## v2.2.1 - 2026-07-24

这一版只修发布工程，不改变阅读器功能和界面。

### 修复

- 固定 electron-builder 的发布资产文件名，避免中文产品名在 GitHub Release 附件中被处理成空前缀或不清晰文件名。
- macOS 发布包将直接生成为 `night-study-2.2.1-mac-arm64.zip` 这类清晰文件名。
- Windows portable 发布包将直接生成为 `night-study-2.2.1-windows-portable.exe` 这类清晰文件名。

## v2.2.0 - 2026-07-23

这一版完成桌面图标与品牌命名更新。应用正式命名为「夜晚的书斋」，并加入日间 / 夜晚双图标。

### 新增

- 新增日间图标：墨绿色版本，作为默认安装与打包图标。
- 新增夜晚图标：靛蓝色版本，作为夜间运行图标。
- 新增 macOS `icon.icns` 与 Windows `icon.ico` 平台图标资产。
- 新增运行时图标自动切换：
  - 06:00–18:00 使用日间图标；
  - 18:00–06:00 使用夜晚图标。

### 改进

- Electron 产品名、窗口标题、页面标题与 README 更新为「夜晚的书斋」。
- `electron-builder` 配置接入正式平台图标。
- README 增加日夜图标说明，明确该功能只影响运行中的应用图标，不改变阅读界面主题。

### 范围说明

- 本版不新增主题切换、夜间模式设置或阅读界面颜色切换。
- 未运行状态的桌面快捷方式 / 安装包图标仍使用日间图标；运行后再按系统时间切换应用图标。

## v2.1.0 - 2026-07-23

这一版重点解决分发体验：让普通用户可以下载桌面版后双击运行，不再需要先安装 Node.js、打开终端或运行脚本。

### 新增

- 新增 Electron 桌面入口 `electron-main.mjs`。
- 新增 `app:start`、`app:dir`、`app:dist` 构建脚本。
- 新增 electron-builder 配置，可生成 macOS / Windows 的桌面 build 产物。
- 新增桌面版 README 说明，区分普通用户下载桌面版与开发者源码运行。

### 改进

- 桌面版会在应用内部启动本地阅读服务，并用独立窗口打开阅读界面。
- 普通用户使用桌面版时不再需要自行安装 Node.js。
- 打包配置暂时关闭 asar，优先保证 PDF.js worker、OCR、WASM 和语言包路径稳定。
- `.gitignore` 忽略 `dist/`，避免把本地构建产物提交进源码仓库。

### 工程化

- 新增 Electron 与 electron-builder 开发依赖。
- 新增 `package-lock.json` 锁定桌面构建依赖。
- 发布前验证：`npm test` 24 项通过，Electron macOS build 可生成。

## v2.0.0 - 2026-07-23

相较于 GitHub 公开版 `v1.0.0`，这一版将「缓缓读 PDF」从一个 Windows 优先的简易自动滚动阅读器，扩展为一个跨平台、本地优先、适合大 PDF 连续阅读的小工具。

### 新增

- 新增 macOS 启动方式：`start-reader.command` 与 `start-reader.sh`。
- 新增空白欢迎页：启动时不再必须预先传入 PDF，可在页面内选择或拖入 PDF。
- 新增六档阅读速度意象：
  - 雪国朦胧
  - 美学散步
  - 长日留痕
  - 流觞曲水
  - 强风吹拂
  - 万物繁盛
- 新增基于当前页附近文字密度的阅读速度估算，支持显示「字/分」与「词/分」。
- 新增可选本地 OCR，用于扫描件或图片页的阅读速度估算增强。
- 新增本地打包的 Tesseract.js 运行文件与四种 OCR 语言包：简中、繁中、英文、日文。
- 新增大文件友好的浏览器文件读取方式，通过 `File.slice()` 与 PDF.js range transport 避免一次性读取整份 PDF。
- 新增纸页底稿与渐进式高清渲染体验，大型文档打开时可以更快看到页面结构。
- 新增渲染调度器，优先渲染当前页和阅读方向附近页面。
- 新增动态缓存预算，根据设备内存与 OCR 状态调整高清画布保留数量。
- 新增诊断接口与前端诊断快照，便于检查加载、渲染、缓存、OCR 与 Range 请求状态。
- 新增回归测试，覆盖 HTTP Range、文件分段读取、页面布局、阅读速度模型、渲染调度与本地服务器行为。

### 改进

- 调速范围由 `5–55 px/s` 调整为 `4–64 px/s`。
- README 从 Windows 使用说明扩展为 macOS / Windows 双平台快速开始。
- 本地服务器支持无 PDF 启动，并通过 `config.json` 返回文件名、文件大小与系统内存信息。
- 静态资源改为流式发送，避免服务器端一次性读取较大资源。
- HTTP Range 处理拆分为独立模块，并补齐 `HEAD`、后缀 Range、非法 Range 与 416 响应语义。
- PDF 页面布局逻辑拆分为独立模块，支持先估算页面位置，再在真实尺寸到达后修正布局。
- 阅读位置通过 anchor 机制保存，页面尺寸修正后尽量维持原来的阅读位置。
- 渲染任务支持取消与 generation 校验，避免旧 PDF 或旧位置的迟到结果覆盖当前页面。
- 控制器视觉表现增强，调速时增加柔和的 `+N/-N` 粒子反馈。
- 对 `prefers-reduced-motion` 做了适配，系统减少动画时停用位移动画和粒子效果。
- 第三方许可说明扩展到 PDF.js 与 Tesseract.js 相关文件。

### 修复

- 修复浏览器选择 PDF 时会将整份文件读入内存的问题。
- 修复大页数 PDF 需要过早访问大量页面导致首屏变慢的问题。
- 修复跳页、换 PDF 或快速滚动时旧渲染任务可能继续占用资源的问题。
- 修复扫描件缺少文本层时无法估算阅读速度的问题，改为允许用户主动启用本地 OCR 增强估算。
- 修复 Windows 启动脚本未显式检查 Node.js 版本的问题。

### 工程化

- 新增模块：
  - `http-range.mjs`
  - `pdf-source.mjs`
  - `page-layout.mjs`
  - `render-scheduler.mjs`
  - `ocr-provider.mjs`
  - `reading-model.mjs`
- 新增测试目录 `tests/`，并在 `package.json` 中加入：

```sh
npm test
```

- 当前测试结果：24 项通过，0 项失败。

## v1.0.0

- 首个公开版本。
- Windows 优先的本地 PDF 自动滚动阅读器。
- 支持打开 PDF、自动滚动、暂停 / 继续、速度调节、页码与阅读进度显示。
- 本地服务器仅监听 `127.0.0.1`，PDF 文件不上传。
