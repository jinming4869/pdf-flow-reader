# v4「希声」工作上下文

更新时间：2026-07-31

## 当前基线

- 公开版本：`v3.0.0`，对应提交 `5f918c2`。
- 当前开发分支：`feature/v4-hisheng`。
- v4 Phase 1 原型检查点：`4647acd`。
- 检查点包含 40 个文件、6692 行新增和 232 行删除。
- 检查点创建前，`npm test` 为 116/116 通过。
- 当前代码版本仍写作 `3.0.0`；这轮不处理发布版本号。

## 六档策略当前检查点

截至 2026-07-31 已保存九个边界清晰的检查点：

- `ace00aa`：六档纯策略、CJK / 日文句界、英文缩写与小数保护、完整配对括号过滤。
- `80188cd`：希声布尔总开关、旧三态偏好迁移、跨档统一取消、前三档基础接线和长日留痕乐符点读。
- `50f24d5`：原生文字 / OCR 段落模型、保守跨页合并和纯 crossing reducer。
- `7ea78da`：流觞曲水两阶段 prepare / play 接线、ready-only 播放、滑动窗口上下文和真实应用调度。
- `29cfa4d`：强风吹拂 / 万物繁盛顶部点句朗读、句子命中与可选文字层、滚动暂停恢复、最后点击优先和硬取消。
- `9f1ffec`：英文窄中缝防串栏、多语 OCR 路由、中日文稳定点句、日文词汇表朗读门槛，以及朗读资格与速度估算解耦。
- `54303c2`：美学散步根据视觉阅读窗口选择 1.5–2.5 倍语速，完整句缩短 / 静默跳过、晚到结果淘汰和动态计划预生成。
- `75854a3`：雪国朦胧把下一页独立加入预取池，正常向前翻页复用；跳读、反向移动和来源变化淘汰旧结果。
- `3e62a21`：雪国朦胧在扫描 PDF 上沿阅读方向补扫下一页，避免初始五页 OCR 窗口耗尽后断声。

当前运行状态已经不再保存 `off / whisper / flow`。`ttsEnabled` 是唯一总开关，具体行为由当前 px/s 对应档位派生。旧 `ttsMode`、`ttsPreferredMode` 和 `ttsConsentGiven` 只在读取时迁移，写回时删除。

已经可执行：

- 雪国朦胧：舒适速度连续随读，预生成当前页后续 chunk 及下一页首个可读 chunk；正常相邻向前翻页不断声，阅读线视觉隐藏。
- 美学散步：过滤页边噪声、脚注、旁注和完整配对括号；根据当前阅读线到 chunk 底部的视觉时间，在 1.5–2.5 倍之间跟随，必要时缩至完整首句或静默跳过。
- 长日留痕：不自动发声；显示阅读线和乐符，`R` 与按钮共用 action，严格读取句子起点位于阅读线下方的最近一句。
- 流觞曲水：内部阅读线越过当前段段首时预生成下一段首句，越过段尾时只播放已经 ready 的结果；迟到、繁忙或上下文失效都安静跳过，不阻塞也不追赶。
- 强风吹拂与万物繁盛：不自动朗读、不显示阅读线；开启顶部“点击以朗读”后，hover 完整句、单击朗读、拖动选择文字。朗读期间暂停自动滚动，自然结束恢复；用户干预不恢复。

仍然如实保留的边界：

- 固定多语样本已经验收，但中文、日文 OCR 仍会出现模型级误字；不把 Tesseract 置信度冒充逐字准确率。
- 中文、日文默认声音质量仍未确定，属于下一阶段模型 PoC，不由本轮英文 Kokoro 真实长跑代替。

自动验证为 273/273；相关 `.mjs` 语法与 `git diff --check` 通过。美学散步阶段的 Electron 目录打包成功；最终真实验收改用隔离源码服务，未关闭占用 `dist/mac-arm64` 的用户旧应用。主线程 reviewer 未发现 P0–P2，当前 `GOAL.md` 已完成。

雪国朦胧最终真实运行证据：

- 使用 584 页、262,204,688 字节的全扫描英文书，本地 OCR 第 11/12 页分别得到 18/11 个可读 chunks；Kokoro q8、`af_heart`、1.18x 生成真实 24 kHz WAV，scheduler 与 audio 失败均为 0。
- 第 30 页朗读中远跳第 100 页，controller generation 立即递增，活动音频停止，旧页结果没有在新页播放。
- 第 101→102 页跨页时预取槽已准备下一页首个合格正文 chunk；越页后 generation、queued 与 stopped 均不增加，prefetchUsed 连续增加。
- 长跑暴露并修复初始 OCR 窗口耗尽：第 105 页仍在朗读时第 106 页自动变为 `ocr-ready`；自然进入第 106 页后，第 107 页继续自动补扫完成。
- 第 105→106 页自然越页时 generation 保持 7、audio stopped 保持 1、失败保持 0；completed 从 16 增至 20，prefetchUsed 从 9 增至 12。
- Tesseract 控制台仍有 core / traineddata 参数不匹配的非致命提示，但 OCR 正常完成；成套升级继续属于模型 PoC。

## 2026-07-31 固定多语样本验收

- 英文双栏论文第 3 页暴露出窄中缝仍会跨栏拼句。现在同一基线上的左右栏会在跨越页面中心且间距超过小中缝阈值时分开，真实页面按左栏后右栏输出，点按句子能进入 warming / highlight。
- 中文扫描书第 50 页使用 `chi_sim+eng`、1600 px 目标宽度和最高 3.2 倍渲染；得到 10 个正文 chunks，加权置信度约 0.855，状态为 `ocr-ready`。点句改用 line bbox 后形成 13 个跨行完整句目标，不再按单字词框碎裂。
- 日文扫描教材第 80 页使用 `jpn+chi_sim+eng`，得到 10 个 chunks，加权置信度约 0.811、句末块比例 0.5，状态为 `ocr-ready`；点按句子能建立单片高亮并进入 1.25 倍准备状态。
- 同一日文教材第 50 页是词汇 / 语法网格。虽然模型置信度约 0.718，但句末块比例只有 1/3，因此状态为 `ocr-poor`、不开放朗读；OCR 字符统计仍用于“字/分、词/分”，避免把估算资格与朗读资格绑定。
- 尝试替换官方 `tessdata_best` 日文模型时，当前浏览器 Tesseract core 报缺少 `DotProductSSE` 符号。二进制已恢复为原模型，工作树没有大模型改动；核心 / 模型配套升级留到模型 PoC，不在内部逻辑阶段冒险混入。
- 当前 OCR 仍会把少量汉字、假名或数字识错。质量评估用于判断“是否足够连贯可读”，不是逐字校对器；模型精度提升继续属于后续中文、英文、日文模型 PoC。

最高两档点句真实运行证据：

- 使用 30 页原生英文 PDF 验证 40 px/s 与 64 px/s：顶部开关显示，底部希声控制隐藏；20 px/s 长日留痕则相反。
- 开启后附近 ±2 页形成可选择文字层；hover 命中稳定 sentence key，并以多个 fragment 高亮跨行整句。
- 自动滚动中点句会立刻建立独立 hold，逻辑播放状态保持；自然结束后恢复原速度。滚轮干预会取消声音和高亮、切为暂停，之后不自动恢复。
- 连续 A → B 点击保持第一次点击前的滚动基线，只让最后一次请求进入 queued / speaking；旧 scheduler、解码与 child inference 经过既有硬取消链路停止。
- 390 × 780 窄屏无横向溢出；纯触屏设备隐藏鼠标专用入口。浏览器控制台 warning / error 为 0。
- 浏览器自动化后端无法在普通页面文字上可靠产生原生 Selection；本轮只据实确认 DOM 使用可选择文字 span，并由 gesture 回归保证拖动和非折叠选择不触发朗读。

流觞曲水真实运行证据：

- 使用 584 页本地扫描书，以 25 px/s 从第 11 页连续运行到第 13 页。
- 页面进入相邻下一页后仍显示“下一段的首句已经备好”，没有因 `[前一页 / 当前页 / 后一页]` 窗口平移丢掉预载。
- 阅读线 DOM 数量为 0，最高两档的点句按钮在本档隐藏。
- 应用控制台 warning / error 为 0。

## Phase 1 已经具备的能力

当前运行链路是：

```text
PDF.js native text / Tesseract OCR
→ TextSegment
→ ReadableChunk
→ reading-line picker
→ TTS controller / scheduler
→ local Kokoro endpoint
→ child process: Kokoro worker
→ Web Audio
```

已经实现：

- native text 与 OCR bbox 到统一坐标的转换；
- ReadableChunk、基础双栏排序和文本噪声降权；
- `off / whisper / flow` 原型状态机；
- Kokoro 本地合成、音频播放、播放锁和下一段预生成；
- 希声 UI、阅读线、快捷键和诊断面板。

这些内容证明了链路可行，但不能视为 v4 已达到产品验收。

## 基础可靠性修复状态

2026-07-25 本轮已完成：

- OCR debounce 现在保留明确的 pending / active 身份；同页状态刷新复用原定时器，换页只替换 payload，不会把启动期限无限后推。
- 页码变化、暂停、换 PDF、关闭希声和 `Esc` 已收敛到统一取消入口。旧请求、预生成、播放锁、阅读线和正在解码的音频一起作废。
- renderer 取消本地 TTS fetch 后，server 会中止对应 runtime 请求；活动推理由 `SIGKILL` 硬停止，下一次请求按需启动新 child。
- Kokoro 模型加载和推理已从 PDF 本地服务主进程移入单并发 child process。队列上限为 8，单次超时为 120 秒；worker 崩溃后排队请求在新 child 中恢复。
- `R` 不再直接操作 controller；PDF、可见控制、档位、希声模式、当前页 chunks 和预热门槛任一不满足时都不会发起合成。
- 新增真实 CPU 忙循环 child 的集成测试，确认推理期间 heartbeat 与 PDF Range 仍可响应。
- 全量自动化为 147/147 通过；独立 reviewer 三轮复核后未发现剩余 P0/P1/P2。

本轮刻意没有处理：

- CJK 首句切分；
- 六档朗读策略、乐符按钮与最高两档点句交互；
- OCR / TTS 重型任务资源仲裁、模型 PoC 和发布工程。

## 文本来源状态机状态

2026-07-25 后续本轮已完成：

- 每页分别保存 native 与 OCR 的质量评估、任务状态和当前选中来源。状态能够区分检查、需要 OCR、已排队、运行、可用、质量不足和失败。
- native 文字达到保守质量门槛时，希声直接可用，不再依赖 OCR 开关。OCR 的 scheduled / running 状态不会关闭希声总入口。
- 当前页 native 缺失或质量不足时，只触发一次附近页扫描；窗口为当前页 ±2、最多 5 页。native-ready 邻页会被跳过，OCR failed / poor 不自动循环。
- native-ready 页保留边缘“扫描页增强”动作。合格 OCR 结果只用于校准速度估算，朗读 chunks 仍由 native 提供。
- Tesseract 7 的 `recognize()` 默认只返回文字。运行时现已显式请求 `blocks`，再从 block → paragraph → line / word 恢复 bbox；否则扫描过程虽完成，却永远不能形成 OCR chunks。
- CLI 参数解析已修复：未传 `--idle-ms` 时不再误删第一个 PDF 位置参数。

真实验收：

- 311 页英文原生文本书的正文页为 `native-ready`，希声按钮可见且可用，OCR 没有自动启动；纯英文速度指标只显示“词/分”。
- 584 页、262,204,688 字节扫描书首屏约 1.1 秒内进入 `ocr-running`，相邻页明确为 `ocr-scheduled`，没有重现 160 ms 刷新饿死。
- 同一扫描书附近页 OCR 产出 15 个带坐标 chunks，质量评估为 `ocr-ready`，页面切到 OCR 来源；封面和短标题页被解释为 `ocr-poor`，且没有自动重试循环。
- PDF 页面已分别用 Poppler 渲染目测；扫描页 OCR 预览与实际纸面正文一致。应用内浏览器控制台无 warning / error。

## 本轮前已确认的基础问题

### P0

- OCR 调度饥饿：状态刷新约每 160ms 重设一次 180ms debounce，扫描任务无法开始。

### P1

- 页内跳转不会取消旧页朗读。
- 浏览器取消 fetch 后，Node 端 ONNX 推理仍继续运行。
- TTS 与 PDF Range 服务共用同一 Node 进程，合成会争抢 CPU 和事件循环。
- `R` 可以绕过 OCR、档位、预热和可见控制的正常入口。
- native text 已经能生成 chunks，但 UI 仍强制要求用户先开启 OCR。
- 当前 Kokoro JS voice set 只有英语 voice；中文和日文回退到 `af_heart`。
- 模型管理器、音效 ducking、跨页页眉统计等模块或接口尚未接入真实运行链路。

## 新一轮产品方向

用户在 2026-07-25 确认：

- 希声行为应由六档物理速度直接决定。
- 原生文本可用时直接开放希声；原生文本不足时才建议或启动附近页 OCR。
- 雪国朦胧连续随读，美学散步高速跟读，长日留痕手动点读，流觞曲水自动读下一自然段段首；强风吹拂和万物繁盛不自动朗读，只在用户开启“点句朗读”后响应鼠标点击。
- 长日留痕显示阅读线和乐符式触发按钮；流觞曲水仍使用内部阅读线判断时机，但不显示阅读线。
- “点句朗读”只出现在最高两档，位置在页面顶部阅读时间估算的左侧。点句后暂停自动滚动，读完恢复；用户中途操作则不自动恢复。
- 先稳定内部逻辑，再考虑模型 PoC 和发布。

完整契约见 `DECISIONS.md`。

## 文档权威顺序

发生冲突时按下列顺序判断：

1. `GOAL.md`：当前完成线与范围。
2. `DECISIONS.md`：已经锁定的产品和技术决定。
3. `PLAN.md`：执行顺序、验收方式和任务边界。
4. `TODO.md`：当前进度。
5. `CONTEXT.md`：当前事实和已知问题。
6. `REVIEW.md`：独立复核与证据。
7. `docs/`：PRD、阶段日志、研究和历史草案。

`docs/PRD_v4.0_HISHENG_TTS.md` 的旧三态模式和速度密度表已被 2026-07-25 的六档新决策覆盖。`docs/TTS_MODEL_DECISION_v4.0.md` 暂停执行，不代表当前默认模型结论。

## 已有真实样本

- 大型扫描书：584 页、约 262 MB，用于大文件、OCR 和长时间阅读。
- 英文社科书与双栏论文：用于 native text、页眉页脚、脚注和栏目顺序。
- 后续还需要固定一份中文扫描正文页和一份日文短文作为永久验收样本；个人原始 PDF 不提交进仓库。
