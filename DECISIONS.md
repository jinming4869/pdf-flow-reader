# 产品与技术决定

## 2026-07-31：雪国朦胧扫描页前向 OCR

- 初次进入扫描页仍使用当前页 ±2、最多五页的 OCR 窗口；连续随读不能假设这个固定窗口足够覆盖整本书。
- 只有希声已开启、雪国朦胧正在自动滚动、当前 OCR workload 空闲且下一页原生文字已确认不足时，才沿阅读方向补扫下一页。
- 下一页已有可朗读文字、已有 OCR 结果或任务、曾经失败、尚未完成原生文字判断，或者当前位于其他档位 / 暂停状态时，都不重复调度。
- 前向 OCR 只保证朗读前方页面及时就绪，不改变物理速度档位、不扩大 picker 到下一页，也不放宽 OCR 朗读质量门槛。

## 2026-07-31：雪国朦胧跨页连续预取

- 当前页 chunks 与下一页 chunks 必须分开传递：当前 picker 只能在当前页选句，预取器才可以看到下一页，避免下一页顶部文字因坐标相近抢走当前朗读。
- 只有已开启的雪国朦胧、普通相邻向前翻页且没有大幅跳读时保留连续会话。倒退、跨页跳转、同页大幅滚动、暂停、换书和跨档仍走统一硬取消。
- 预生成身份不只比较 source/page/chunk key，还比较实际朗读文本、语速和美学散步计划签名。下一页从 OCR 改为 native 或内容被校正时，即使 key 恰好相同也不能播放旧音频。
- 跨页预生成向 Provider 记录目标 chunk 的真实页码。调度压力链必须保持每个 chunk 只合成一次、没有重复追赶和并发重叠。

## 2026-07-31：美学散步跟随视觉窗口

- 视觉滚动仍是主时钟。每个候选 chunk 使用当前阅读线、chunk 底部、页面实际像素高度和当前 px/s 计算剩余视觉时间，不让音频反向控制页面。
- 语音时长使用可解释的语言启发式估算：中日韩字符按约 300 字/分、拉丁词与数字按约 165 词/分估算 1 倍时长，再把目标语速限制在 1.5–2.5 倍。
- 给本地合成保留 0.65 秒准备余量。整段在 2.5 倍仍来不及时，只保留第一个有完整句界的句子；首句也放不下则不启动合成。
- 每个预生成槽保存目标 chunk 自己的朗读文本、动态语速和最晚安全起点。同档微调 px/s 后，如果文本或语速计划变化，已有结果也作废重做；若合成完成时已经越过最新安全起点，直接释放播放锁且不交给音频层。
- 缺少页面高度或物理速度时诚实降级到 1.5 倍，不虚构动态精度。界面只说明“1.5–2.5 倍跟随纸页”，不把内部估算包装成精密测量。

## 2026-07-31：固定多语样本与 OCR 资格

- 英文双栏不能只用页面宽度 8% 作为横向断行阈值。文字跨越页面中心且保留可见窄中缝时，必须拆成左右两条阅读流；点句读取顺序仍为左栏后右栏。
- OCR 语言组合按文档标签和首个可靠识别结果学习。英文使用 `eng`，简中使用 `chi_sim+eng`，繁中使用 `chi_tra+eng`，日文混合教材使用日文优先的 `jpn+chi_sim+eng`；物理滚动档位不受语言路由影响。
- 中日文 OCR 的 word bbox 容易碎裂和错序，点句使用 line bbox 形成完整句；英文 OCR 继续使用 word bbox 保留精确点击。扫描边框误识别成的行首尾 `|` 可以清除，真实内部栏分隔保留给质量判断。
- OCR “足够估算文字密度”和“足够朗读”是两项资格。置信度、噪声、bbox 覆盖合格的词汇表可以校准字/词每分钟，但日文列表若缺少稳定句末，不生成朗读来源。
- Tesseract 的 weighted confidence 不是逐字准确率。真实样本仍有误字时应如实记录，不以调低质量门槛掩盖。
- 当前 Tesseract browser core 与官方 `tessdata_best` 日文模型不兼容，会因缺少 `DotProductSSE` 符号中止。内部逻辑阶段保留已验证模型；core、模型和包体积必须在后续模型 PoC 中成套比较。

## 2026-07-31：最高两档点句朗读

- 强风吹拂与万物繁盛复用唯一的 `ttsEnabled` 总开关，但把它放在页面顶部阅读时间估算左侧；本档隐藏底部希声控制。其他四档不显示顶部入口，`R` 仍只属于长日留痕。
- 点句使用显式 sentence target，不再经过阅读线 picker。native text 先通过 PDF viewport 转为 top-down 坐标；OCR 点句优先使用 word bbox，无 word 时退回 line。
- 一个完整句子可以由多个 fragment 组成。命中先取直接 fragment，再允许有限半径的最近句；双栏 gutter 不跨栏吸附，页边、参考文献、表格、公式等仍经过正文 policy 过滤。
- 自动滚动的视觉状态与点句临时 hold 分离：点击不会把总播放状态改成暂停。自然结束只在单调 request token 仍有效、用户原本在播放且中途没有干预时恢复点击前速度。
- 新点句继承第一次点击前的滚动基线，并立刻取消旧音频、异步解码、scheduler 与实际 child inference。旧请求的迟到结果和旧音源结束事件都不能恢复新请求。
- 鼠标主键短按同一句才触发；超过 6 px 的拖动、已有 Selection、修饰键、触摸和按下/抬起跨句都保持安静。纯触屏设备隐藏鼠标专用入口，混合设备只要存在 hover 能力仍可显示。
- 高亮分为 hover、warming / queued 与 speaking 阶段；活动请求期间 hover 不能覆盖正在朗读的句子。视觉采用淡纸黄与墨绿，并遵守 reduced-motion。
- 英文 initials 只在更明确的人名上下文中合并；Appendix / Section / Figure / Table 等单字母学术标签仍可作为真实句末。

## 2026-07-26：流觞曲水的时序与上下文

- 自然段是独立于旧 ReadableChunk 的结构。OCR 优先保留识别引擎的 paragraph id；原生文字使用保守的行距、缩进、短末行、标题和双栏启发式。
- 跨页段落只有在同一来源、正文角色、页尾 / 页首位置和水平缩进同时吻合，且前页没有完整句末时才合并；宁可多切一段，也不把两段错误粘连。
- 段首时只发起下一段首句的 `prepare`，段尾时只允许同步播放已经 ready 的同 key、同 context 结果。pending、stale 或 playback busy 都立即丢弃，不等待、不追赶、不重叠。
- 普通相邻向前翻页使用滑动窗口上下文。窗口从 `[p-1,p,p+1]` 变为 `[p,p+1,p+2]` 时，只比较重叠页的有效来源和 revision；重叠内容没变就保留 prepared lead。
- 当前窗口的有效 native / OCR 来源变化、段落缓存 revision 变化、倒退、远跳、暂停、换文档或 controller generation 变化会作废旧预载。非选中来源和窗口外 OCR 完成不能扰动当前朗读。

## 2026-07-26：六档策略迁移与诚实交付边界

- `ttsEnabled:boolean` 是唯一持久总开关；旧 `ttsMode` 只用于一次性迁移，不能保留兼容循环或让 `H` 回到三态。
- 档位名称和朗读 policy 只由物理速度档位决定；翻页文字密度不会改变朗读档位。
- 跨档时统一取消旧音频、合成和预取，但保留总开关状态；同档内微调 px/s 不取消会话。
- 长日留痕的“下方最近一句”以句子估算起点为准：句子起点必须位于阅读线之下，阅读线穿过的句子不算“下方”。
- 美学散步已由 `54303c2` 解除固定 1.5 倍门禁；运行时必须取得页面高度与 px/s 才能动态跟速，否则明确走 1.5 倍 fallback。
- 流觞曲水与最高两档在完整触发器尚未实现时必须保持安静、显示“仍在接线”；不能复用旧 flow 定时朗读来冒充新行为。流觞曲水已由 `7ea78da` 解除该门禁，最高两档已由 `29cfa4d` 解除。
- 顶部“点句朗读”按钮不能先做假入口。它应与句子级命中、拖选区分、暂停/恢复令牌、柔和高亮和最后点击优先同时交付。

## 2026-07-25：v4「希声」工作边界

- v4 先修内部逻辑，再重新做模型 PoC，最后才处理发布工程。模型和发布不得与基础状态修复并行扩张。
- 当前原型保存在 `feature/v4-hisheng` 的 `4647acd`。后续修改应保持小步提交，不能继续堆在未提交的 `main` 工作区。
- 根目录的 `GOAL.md`、`PLAN.md`、`CONTEXT.md`、`TODO.md`、`DECISIONS.md` 和 `REVIEW.md` 是执行依据；`docs/` 中的 PRD、日志和调研按历史或证据使用。
- Kokoro 暂时只是已经跑通的英文实验 Provider，不代表中文、日文默认模型已经决定。
- 本阶段不触发模型路线更换，也不制作 v4 发布包。

## 2026-07-25：文本来源与 OCR

- native text 是第一来源。质量足够时直接开放希声，不要求用户先开启 OCR。
- native 文本缺失、过少或结构差时，应用才建议或自动调度当前页附近 OCR。
- OCR 是文本补救和速度估算增强，不是朗读资格开关。
- native-ready 页允许用户手动扫描以校准速度估算；只要 native 质量仍合格，希声朗读来源保持 native，不因手动 OCR 改写。
- 页面必须暴露可解释状态：native 可用、native 较差、OCR 等待、OCR 运行、OCR 可用、无可读文本或失败。
- OCR 调度必须有 pending 身份；重复状态刷新不得把同一任务永久往后推。
- Tesseract 7 运行时必须显式请求 `blocks` 结果，并从 block 层恢复 line / word bbox；只拿 `text` 不能满足速度估算与朗读定位。

## 2026-07-25：TTS 隔离与取消

- TTS 推理不得继续和 PDF Range 服务运行在同一执行上下文。优先使用 child process，取得比普通 fetch abort 更可靠的硬取消和故障隔离。
- 一个事件必须同时停止音频、清空播放锁、作废 generation、取消队列和预生成，并停止活动推理。
- 页内远跳、暂停、换 PDF、关闭希声、切换朗读策略和选择新的点读句子都属于取消边界。
- 如果底层 ONNX 推理不能协作取消，可以终止并重建子进程；不能只在前端丢弃结果。
- 同一时间只允许一个活动 TTS 推理。OCR 与 TTS 的重型工作要有资源仲裁。
- `R`、点击按钮和自动触发必须调用同一公开 action，不能绕过可用性、预热或档位规则。

## 2026-07-25：六档朗读契约

- 保留希声总开关。具体朗读方式由 px/s 对应的档位决定，不再要求用户理解彼此重叠的“低声 / 随读”三态。
- 实际阅读线始终可用于内部同步；是否把阅读线画出来由档位决定。

| 档位 | 自动朗读 | 阅读线视觉 | 朗读内容与触发 |
| --- | --- | --- | --- |
| 雪国朦胧 | 是，连续 | 隐藏 | 舒适语速随读，播放当前段时预生成下一段；不读页眉页脚 |
| 美学散步 | 是，连续 | 隐藏 | 1.5–2.5 倍跟读；过滤脚注、注释、成对括号内容和页眉页脚；跟不上时缩短或跳过 |
| 长日留痕 | 否 | 显示 | 乐符式按钮或 `R` 触发阅读线下方最近一句；读完即停，不触发就安静 |
| 流觞曲水 | 是，稀疏 | 隐藏 | 只读自然段段首；越过当前段段首时预生成下一段段首，越过当前段段尾时播放 |
| 强风吹拂 | 否 | 隐藏 | 仅在“点句朗读”开启后，点击 PDF 中的句子朗读 |
| 万物繁盛 | 否 | 隐藏 | 仅在“点句朗读”开启后，点击 PDF 中的句子朗读 |

- 美学散步过滤括号内容时只处理结构完整的成对括号；不因不完整括号删除整段。
- 长日留痕的乐符不是装饰。它必须有键盘等价操作、清楚的可用状态和无文本反馈。
- `R` 只在长日留痕有效，并与乐符按钮共用同一个动作。
- 流觞曲水的预生成不能阻塞滚动；迟到就跳过，不能追赶或重叠。
- “点句朗读”只在强风吹拂和万物繁盛出现。它在这两档中就是希声的可见开关，不与底部总开关重复显示。开关放在页面顶部节奏信息区，位于“以当前速度，估算还需阅读约……”的左侧。
- 开启点句朗读后，鼠标悬停以柔和反馈标出最近的完整句子；单击朗读，拖动仍保留文字选择能力。
- 点句时暂停自动滚动，并以淡纸黄或墨绿色短暂高亮整句。朗读结束后恢复点击前的速度；如果用户中途滚动、调速、暂停或切换页面，则不自动恢复。
- 新的点句请求必须取消上一句的音频和实际推理，以最后一次点击为准。普通 PDF 使用 native text 坐标，扫描页使用附近页 OCR 坐标；文本未就绪时显示轻量状态，不发起空朗读。
- 本阶段不做逐词高亮。视觉阅读仍是主时钟，声音跟随页面，不反向驱动滚动。

## 2026-07-21：第二步阅读速度

- 数字的作用是横向比较，显示“字/分”与“词/分”，不做阅读测试或完成时间承诺。
- 档位意象跟随物理滚动速度，文字密度只影响数字估算。这使后续音乐和色彩不会因翻页突然换档。
- 用附近五页平滑单页版式差异，并将重计算限制在翻页时。
- 本节记录的是 v3 阅读速度估算阶段的旧交互：OCR 当时由用户选择、默认不启动。v4「希声」的文本补救已改为 native-first；native 不足时可以自动调度当前页附近最多 5 页 OCR。识别仍只在本地进行，支持简体中文、繁体中文、英文和日文。
- OCR 入口称为“扫描页增强估算”，而不是泛泛的“文字识别”。它只服务于扫描件、图片页的“字/分、词/分”估算，不暗示搜索、复制或编辑功能。
- 数字可以进入中文/日文的“字”计数，但不参与语言判定。只有真实的汉字、平假名或片假名能触发“字/分”，避免英文表格中的年份和页码被误判成中文。
- Tesseract.js 7 在同一 worker 中同时初始化四个模型时出现了稳定的语言数据异常。运行时因此使用“简中 + 英文 + 日文”主组合，检测到繁体特征后在同一 worker 内切换为“繁中 + 英文 + 日文”。两组本地模型和切换路径均已通过四语扫描图实测。
- 调速反馈是圆润文字粒子，不是精密仪表或矢量图标。系统设为减少动画时不播放粒子。
- 本步只为音频/色彩留下 theme key，不提前实现主题切换。

## 2026-07-22：第三步长文档性能

- 产品保持全自动，不增加“性能模式、节能模式、画质模式”等用户开关。
- 大文件必须按区间读取。通过文件选择器打开时用 `File.slice()`，通过启动器打开时用 HTTP Range；两条路径都禁止后台顺序吞完整文件。
- 当前页最终清晰度优先于节省一点内存。允许先显示纸页底稿或临时预览，但停留后必须升到最高 DPR 2。
- 缓存同时受页面数和 canvas 像素预算约束。基础页面预算按 8/16/32 GB 设备约为 7/10/14 页，未知设备按 10 页。
- 被逐出的页面只删除高清画布，保留尺寸 shell、原生文字统计和 OCR 统计，回看时重新显影。
- 页面调度偏向阅读方向；跳页时取消旧任务，当前页永远拥有最高优先级。
- “纸页底稿”使用准确或估算的纸张尺寸、低对比度页码和单个呼吸圆点。普通翻页不显示加载文案，首次首屏过慢时才显示“正在显影第一页”。
- 用户提供的 584 页、约 262 MB PDF 是第三步真实主样本。1000 页、500 MB 仍是设计目标，未取得对应样本前不声称已实测。
- 本轮不加入缩略图侧栏、搜索、目录导航、缩放或密码 PDF。
- HTTP 路径继续使用 PDF.js 已验证的原生 Range，并由本地服务统计实际请求与发送字节；文件选择器使用带首块预读的 `PDFDataRangeTransport`。浏览器自管 HTTP transport 在真实样本上会停在首块，已否决。
- Range chunk 固定为 256 KiB。真实样本将它提高到 1 MiB 后反而读取了整份 262 MB 文件，虽然请求数减少，但违背按需读取目标，因此不采用。
- 打开文件的 generation、取消令牌和纸页底稿必须先于首块读取建立。最后一次选择永远获胜，晚到的冷文件不得覆盖新文件。
- 8 GB 设备开启 OCR 后仍保留 7 个页面名额，以保证当前页 ±2 和阅读方向额外 2 页；OCR 通过收紧像素预算节省内存。
