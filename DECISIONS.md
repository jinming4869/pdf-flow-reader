# 公开设计决策

本文记录理解当前实现所需的稳定产品与技术决策。

## 本地优先

- PDF、OCR 文字、朗读文字、模型和阅读记录默认只在本机处理。
- 桌面版携带正式支持语言所需的离线模型与运行时。
- 本地服务绑定 `127.0.0.1`，不对局域网开放。

## 文本来源

- 原生 PDF 文字层是第一来源。
- 原生文字缺失或质量不足时，才按需识别当前页附近的扫描页。
- OCR 的“足够估算文字密度”和“足够朗读”分别判断。
- 原生文字与 OCR 都转换为统一的 `TextSegment` 和 `ReadableChunk` 结构。

## 阅读策略

- 物理滚动速度划分为六个档位。
- 每个档位由纯策略描述自动朗读、阅读线、点句、内容过滤和预取行为。
- 长日留痕采用显式单句触发；最高两档采用鼠标点句；其他档位按各自节奏自动或预备朗读。
- 新的点句、跳页、暂停、换文档和策略变化必须取消不再适用的音频与推理。

## 多语言朗读

- 英文使用本地 JavaScript/ONNX 运行时。
- 中文和日文使用打包后的独立 worker。
- 中英、日英混排先按文字片段路由，再拼接为连续 PCM 音频。
- 正式运行路径不依赖首次使用时联网下载模型。

## 大文件与渲染

- 启动器路径使用 HTTP Range，页面选取文件使用 `File.slice()` 与 range transport。
- 页面先建立轻量纸页结构，再渲染当前页附近的高清画布。
- 调度器优先当前页与阅读方向，并允许取消过期任务。
- 缓存按设备内存、页面像素量和 OCR 状态动态调整。

## 发布与许可

- Node 与 Python 依赖均使用锁文件。
- 模型资产下载后校验固定大小和 SHA-256。
- Release 只有在双平台构建与打包冒烟测试通过后才发布。
- 第三方代码、模型和字典许可记录在 [`THIRD_PARTY_NOTICES.md`](./THIRD_PARTY_NOTICES.md) 及运行时许可文件中。

历史设计资料位于 [`docs/`](./docs/)；若与当前代码或版本说明冲突，以当前实现、README 和 CHANGELOG 为准。
