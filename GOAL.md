# 项目目标

「夜晚的书斋」是一款本地优先的 PDF 连续阅读器，目标是在不上传文档的前提下，将自动滚动、文字识别和离线朗读组织成连贯的阅读体验。

## 当前目标

- 让普通用户可以在 macOS Apple Silicon 与 Windows x64 上直接运行桌面版。
- 对原生文字 PDF 优先使用 PDF 文字层，对扫描页按需使用本地 OCR。
- 让六档阅读速度同时控制视觉节奏和朗读行为。
- 在跳页、暂停、换文档或停止朗读时可靠取消旧任务。
- 对大型 PDF 使用分段读取、渐进渲染和受控缓存。
- 将模型、OCR、阅读位置与偏好保留在本机。

## 质量边界

- 自动化测试覆盖文本处理、OCR 调度、Range 读取、渲染调度、朗读策略与取消语义。
- 发布包必须包含离线运行资源、第三方许可和完整性校验文件。
- 对尚未覆盖的平台、未签名状态和模型局限保持明确说明。

## 当前不包含

- PDF 编辑、批注与文献管理
- 云同步与账户系统
- 自动更新
- Intel macOS 与 Linux 正式安装包
- 云端 TTS 作为默认路径

版本功能与限制以 [`README.md`](./README.md) 和 [`CHANGELOG.md`](./CHANGELOG.md) 为准。
