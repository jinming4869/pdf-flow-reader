# 当前目标：稳定 v4「希声」的内部逻辑

状态：已完成（2026-07-31）
工作分支：`feature/v4-hisheng`
Phase 1 原型检查点：`4647acd`
最高两档点句朗读检查点：`29cfa4d`
美学散步动态跟速检查点：`54303c2`
雪国朦胧跨页实现检查点：`75854a3`
雪国朦胧前向 OCR 与真实长跑检查点：`3e62a21`

## 目标

在不更换默认 TTS 模型、不制作发布包的前提下，把「希声」从“英文 PDF 可以发声的工程原型”推进到“基础状态可靠、六档行为清楚、可用真实 PDF 验收”的内部版本。

这轮工作的重点不是增加音色，而是让文本、阅读线、声音和取消语义一致。

## 完成线

同时满足下列条件，当前目标才算完成：

1. 扫描 PDF 开启或自动触发 OCR 后，任务能够真正启动，不再被状态刷新永久推迟。
2. 原生文本质量足够的 PDF 可以直接使用希声；只有原生文本不足时才建议或自动调度附近页 OCR。
3. 跳页、暂停、换 PDF、关闭希声、切换朗读策略或点读新句子时，旧音频立即停止，旧合成不会继续占用主阅读进程。
4. TTS 推理从 PDF 本地服务主进程中隔离；服务端能够硬取消活动推理，PDF Range、滚动和页面渲染不被拖住。
5. `R` 和所有可见控制都经过同一套可用性、预热、取消和档位规则。
6. 六档朗读策略按 `DECISIONS.md` 实现，并有针对正文、页眉页脚、脚注、括号内容、自然段段首、阅读线点读和鼠标点句朗读的永久测试。
7. 使用原生英文 PDF、英文双栏论文、中文扫描书和日文样本完成真实运行复核。
8. `PLAN.md`、`TODO.md`、`CONTEXT.md`、`DECISIONS.md` 与 `REVIEW.md` 和实际代码状态一致。

## 本轮不做

- 不重新选择或替换默认 TTS 模型。
- 不把 OpenAI、MiniMax 或其他在线 Provider 接入正式 UI。
- 不处理 v4 版本号、签名、安装包、GitHub Release 或发布资产。
- 不做逐词高亮、全文音频导出、播客模式、批注和文献管理。

模型 PoC 和发布工程只有在上述完成线通过后才重新进入计划。
