# PDF Flow Reader 迭代计划

Slogan：让 PDF 阅读和轻吻纸质书一样好玩。

## 迭代顺序

1. 【已完成】一键进入：无参数启动后先进入界面，支持页面选择和拖入 PDF，并补齐 macOS/Windows 启动说明。
2. 【已完成】有温度的数字化速度：六档阅读意象、字/分与词/分估算、调速粒子动画，以及可选的本地 OCR。
3. 【已完成】长文档性能：分段读取、按需渲染、任务取消、动态缓存、纸页底稿和大文件测试。
4. 【待开始】伴读音：速度档位、原创/合规音频、淡入淡出和默认静音。

## 第二步范围

### 速度档位

| 物理速度 | 档位名 | 主题键 |
| --- | --- | --- |
| 4–8 px/s | 雪国朦胧 | `snow-mist` |
| 9–14 px/s | 美学散步 | `aesthetic-walk` |
| 15–21 px/s | 长日留痕 | `long-day` |
| 22–31 px/s | 流觞曲水 | `winding-stream` |
| 32–44 px/s | 强风吹拂 | `strong-wind` |
| 45–64 px/s | 万物繁盛 | `all-things-flourish` |

滑杆范围 4–64 px/s，步长 1，默认 16 px/s。档位只由 px/s 决定，不随 PDF 文字密度变化，为后续音乐与页面氛围保留稳定的主题键。

### 阅读速度估算

- 以现有阅读线所在页为中心，取当前页±2 页组成最多 5 页的窗口。仅翻页或 OCR 结果到达时重新计算，不进入每帧滚动路径。
- 文字密度 = 窗口内有效单位数 / 窗口内页面的 CSS 高度总和；估算速度 = 文字密度 × px/s × 60。
- “字”统计汉字、日文平假名/片假名和数字，排除空格与标点；“词”统计英文词，排除标点。数字只有在页面已由真实 CJK 字符判为 CJK 或混排时才加入“字”计数。
- 语言判定只使用真实 CJK 字符与英文词，年份、页码等数字不参与判定。CJK 与英文各至少占标准化单位的 15%，且各至少 20 个有效单位时，同时显示“字/分”与“词/分”；否则只显示占主导的一项。
- 数字前保留“≈”，明确它是版式密度估算，不把它表述为真实理解速度。不实现目标完成时间或倒推速度。

### 交互与动画

- 控制器同时显示档位名、估算字/词每分钟和精确 px/s。
- 用户手动增速时，数字轻微膨胀/弹起，吐出圆润、可爱的 `+1` 文字气泡；减速时数字压扁，吐出 `-1`。快速拖动可合并为 `+N/-N`。粒子不使用 SVG。
- 翻页导致密度变化时，数字在约 400 ms 内柔和呼吸/渐变，不发射粒子。
- `prefers-reduced-motion` 下取消位移、弹跳和粒子，保留即时数字变化。

### 可选本地 OCR

- 页面边缘放置低存在感的“扫描页增强估算”按钮，只有用户主动开启才加载 OCR。悬停、聚焦时说明：它只适合扫描件或图片页，用于提高“字/分、词/分”的估算精度，普通文字 PDF 通常不需要。
- OCR 引擎、WASM 与简中/繁中/英文/日文模型全部本地打包，运行时不访问外网。语言包按需加载，不阻塞普通文字型 PDF。
- 开启后持续到当次阅读结束或换 PDF。每次处理当前页±2 页，当前页优先、单并发，结果会话内缓存；换 PDF 后清空。
- 展示可中断的进度，例如“正在扫描附近页 2/5”，完成后显示“附近扫描已用于估算”。OCR 失败不中断 PDF 阅读，并可关闭重试。

## 第二步验收标准

- 六个档位边界、默认值和 theme key 通过单元级边界检查。
- 中文、英文、中英混排、日文和纯扫描页面能得到符合规则的显示项；标点与空格不计数。
- 滚动每帧不调用 `getTextContent()` 或 OCR；只在页码变化后调度附近五页。
- 无 OCR 时普通 PDF 仍然完全本地工作；开启 OCR 后断网也能完成四语识别。
- 桌面端与窄屏不遮挡 PDF 主体，控制器可操作；减少动画设置生效。
- 完成 JavaScript/shell 语法、`git diff --check`、浏览器实际打开 PDF、调速、翻页、OCR 与窄屏验证。

## 本轮验证

- Node.js 和 shell 语法检查通过。
- 无参数启动返回 `config.fileName = null`，首页显示欢迎页。
- 带 PDF 路径启动可渲染页面、显示文件名和控制栏。
- 服务只监听 `127.0.0.1`，不依赖 npm 包。
- 阅读模型已补成永久回归测试，覆盖档位边界、语言计数、混排阈值、单位公式，以及“英文表格含大量数字时仍只显示词/分”。
- 简中/英文/日文与繁中/英文/日文两组本地 OCR 在四语扫描样张上识别成功。
- OCR 静态资源、PDF 和配置的本地 HTTP 路由与 CSP 检查通过，运行源码无外部 URL。
- 独立 reviewer 在修正 OCR 抢占式取消和加载期按钮问题后复核通过。
- 应用内浏览器已用真实英文 PDF 完成桌面与 319 px 窄屏检查：只显示“词/分”，扫描增强按钮保持贴边，说明与完成状态互不重叠，浏览器控制台无错误。

## 第三步范围

### 目标与样本

- 常见目标：约 300 页、50 MB；普通电脑上约 2 秒出现清晰第一页并可开始阅读。
- 极端设计目标：约 1000 页、500 MB，扫描版约占 70%。1 秒内先出现纸页底稿，5 秒内出现清晰第一页是软目标；文件结构、单页图片大小或 iCloud 下载会影响实际时间。
- 真实主样本：用户提供的 584 页、约 262 MB 本地 PDF。它用于大型扫描件实测，但不等同于 1000 页、500 MB 已实测。
- 第一优先平台是 macOS 的 Chrome 和 Safari；第二优先是 Windows 的 Chrome 和 Edge。移动端只保证基础可用。

### 分段读取

- 文件选择器和拖放不再调用整文件 `arrayBuffer()`；使用 `PDFDataRangeTransport` 与 `File.slice()` 按 PDF.js 请求读取区间。
- 本地服务器的 `/document.pdf` 保留 HTTP Range，补齐 `HEAD`、开放范围、后缀范围、非法范围和中断处理。PDF.js URL 来源关闭顺序流和自动全量预取。
- 运行时不把 500 MB 文件整体复制到 JavaScript 内存。静态大文件改为流式响应，避免加载 OCR 模型时阻塞本地服务器。

### 纸页底稿与首屏

- 打开 PDF 后立即显示一张温润的纸页底稿。底稿保留页面尺寸和页间距，中央只有低对比度页码；当前正在渲染的纸页带一个缓慢呼吸的小圆点。
- 其余页面先按第一页比例建立轻量 shell，不等待逐页 `getPage()`。真实尺寸在页面进入附近区域时修正，并保持当前阅读页和阅读线相对位置不跳。
- 普通翻页不显示传统转圈。只有首次清晰首屏明显变慢时，才显示“正在显影第一页”。高清画布完成后柔和淡入；减少动画时直接替换。

### 渲染调度与清晰度

- 当前页和可见页是最高优先级，阅读方向的下一页优先于身后页面。稳定阅读时普通页面最多双并发。
- 跳页、反向滚动或换 PDF 时，取消目标窗口之外的 `renderTask`；迟到结果同时校验文档 generation 与任务身份，不得覆盖新位置。
- 正常连续阅读直接渲染高清。首次遇到极重页面或大幅跳转时可以先显示底稿或临时预览，但页面停留后必须晋升到 `min(devicePixelRatio, 2)` 的最终画质，不能永久模糊。
- 当前页最终画质是硬要求。异常大画布如果超出浏览器能力，应明确报错或采用安全降级，不能悄悄把低清预览当最终结果。

### 动态缓存与内存

- 自动读取设备内存档位：8 GB 约保留 7 页高清画布，16 GB 约 10 页，32 GB 及以上约 14 页；无法取得时按 10 页处理。
- 当前页 ±2 固定为热区，沿滚动方向额外保留至少 2 页，其余名额按距离与方向填充。OCR 启用后适当缩小画布缓存。
- 页数上限之外再设 canvas 像素预算，防止超大纸张只靠页数限制仍然失控。
- 逐出时取消渲染、把 canvas 压到 `1×1` 后移除，并清理闲置 page proxy；shell 和纸页底稿保留。原生文字统计与 OCR 文字统计保留到阅读结束。
- 当前页定位由线性扫描改成布局索引二分查找，滚动帧内不遍历 1000 个页面节点。

### OCR 协同

- OCR 仍然只处理当前页附近 ±2 页，当前页优先，使用独立临时画布；完成后立即释放画布，只保存轻量文字统计。
- 快速跳页时取消旧位置识别，新位置拥有优先级。已经完成的 OCR 结果本次阅读不重复。
- OCR 不占用当前页高清渲染槽。底层识别无法轻量停止时可以终止 worker，但需要防抖，避免连续跳页反复加载模型。

### 可观测性

- 不增加面向用户的性能设置。提供本机只读诊断快照，记录首屏阶段时间、Range 请求、渲染完成/取消数、当前 canvas 页数与像素、缓存档位、热区、队列和 OCR 状态。
- 诊断只用于自动化和人工验收，不上传、不出现在正常界面。

## 第三步验收标准

- 50 MB 级普通 PDF 约 2 秒出现清晰首屏；584 页、262 MB 主样本先出现底稿，5 秒清晰首屏作为软目标。
- CLI 路径和文件选择器都按需读取；服务器实际返回 `206`，文件选择器不出现整文件 `arrayBuffer()`。
- 连续阅读、跳到文末再返回、快速往返和切换 PDF 时，canvas 数量受页数与像素预算约束，不随已读页数线性增长。
- 跳页后旧任务取消，目标页优先；被逐出的页面能重新显影并最终达到 DPR 2。
- OCR 开启后跳页，旧识别停止，新位置附近五页优先，已完成统计仍命中缓存。
- 页面尺寸修正和窗口缩放后，当前阅读页与阅读线位置保持稳定。
- 8/16/32 GB 模拟档位分别得到 7/10/14 页基础预算；异常大页仍受像素预算限制。
- Range、调度、缓存、布局索引和文件分段读取有永久自动化测试；真实主样本完成浏览器视觉、清晰度、首屏、跳页和内存趋势检查。
- 缩略图侧栏、搜索、目录导航、缩放和密码 PDF 不在第三步范围内。

## 第三步验证

- 永久回归测试 24/24 通过，覆盖 Range 解析与服务器响应、File transport 的真实 PDF.js 打开、布局索引与锚点、缓存档位、方向热区、短跳抢占、取消后立刻跳回，以及像素预算逐出。
- 用户提供的 584 页、262,204,688 字节扫描 PDF 在本机已驻留后的重复测试中，纸页底稿约 0–1 ms 出现，元数据约 0.53–0.70 秒，首个 DPR 2 高清画布约 0.63–1.84 秒。
- HTTP 路径稳定在约 580 次 256 KiB Range、151,842,064 字节区间数据；一次 PDF.js 能力探测的全量 URL 请求在约 320 KiB 后取消，没有把 262 MB 文件完整送入页面。File 路径直接以同一真实样本验证：575 次 `slice()`，读取 150,531,344 字节，成功识别 584 页。
- 直接跳到约第 556 页后稳定保留 8 张画布、42,743,680 像素，当前页 ±2 完整；返回开头后为 9 张画布、47,988,880 像素。画布没有随已读页数累积。
- 扫描增强在第 420 页运行中快速改跳第 337 页，旧任务停止，新位置五页完成；纯英文扫描结果只显示“词/分”。
- 暂停后将窗口从 900 px 恢复到 1280 px，仍停在第 295 页，阅读线相对位置从 0.5267 变为 0.5032。
- 真实浏览器无 warning/error。独立 reviewer 在两轮修正后确认无 P0–P2 问题。
- 1000 页、500 MB 仍是设计目标，并未冒充真实样本已验证。
