# 夜晚的书斋 v4「希声」执行计划

状态：当前 v4「希声」内部逻辑目标已完成；下一阶段转入中文、英文、日文模型 PoC
当前目标：见 `GOAL.md`
事实基线：见 `CONTEXT.md`
锁定决策：见 `DECISIONS.md`

## 0. 工程状态整理

状态：已完成

- 从 `v3.0.0` 主干创建 `feature/v4-hisheng`。
- 将已有 v4 Phase 1 原型保存为检查点 `4647acd`。
- 重建项目记忆文件，并建立 v4 文档索引。
- 将旧 PRD、模型草案和阶段日志标记为不同性质的资料，避免把历史草案误当成当前计划。

## 1. 先建立失败测试

状态：基础 bug、native-first、CJK 句界、段落与点句测试已完成

在修改实现前补齐下列永久回归：

1. 连续状态刷新快于 debounce 时，OCR 仍只启动一次。已完成。
2. 从一个有活动朗读的页面跳到远页，旧音频停止，旧结果不得播放。已完成。
3. 关闭希声、暂停、换 PDF 使用同一个取消入口。已完成；跨档策略切换和新的鼠标点句也已接入。
4. `R` 在功能不可用时不触发模型下载或推理。已完成。
5. native chunks 可用时，希声入口不依赖 `ocrEnabled`。已完成。
6. 普通中文、日文句号后没有空格时，首句切分仍然正确。已完成，同时覆盖闭引号、英文缩写和小数。
7. “点句朗读”只在强风吹拂和万物繁盛显示，其他四档不会误触发。已完成。
8. 点句后自动滚动暂停；朗读自然结束时恢复原速度，用户中途操作时不恢复。已完成。
9. 连续点击两句话时，前一句的音频与实际推理被取消，只播放最后一次点击。已完成，覆盖请求替换、迟到结果、解码后播放和实际推理取消。

本阶段的验证只使用可注入的 fake provider、fake clock 和 fake OCR scheduler，不触发真实模型下载。

## 2. 修复 OCR 与文本来源状态机

状态：已完成

### OCR 调度

- 为 debounce 增加明确的 pending page / request 状态。
- 同一页已有待启动任务时，不因重复状态 tick 重置 timer。
- 页码变化时只替换真正过期的 pending 任务。
- 为 `idle / scheduled / running / completed / empty / failed / cancelled` 建立可诊断状态。

### native text 与 OCR 选择

页面文本状态统一为：

```text
checking-native
native-ready
ocr-needed
ocr-scheduled
ocr-running
ocr-ready
ocr-poor
ocr-failed
ocr-suppressed
```

- native chunks 达到质量门槛时，直接开放希声。
- native 文本过少、碎裂或缺失时，显示温和说明，并自动调度以当前页为中心、最多 5 页的附近 OCR。
- 已有 native chunks 的普通 PDF 不启动 OCR。
- OCR 只覆盖质量更差的 native 结果，不把“用户按过 OCR”当成朗读资格。
- native-ready 页仍可手动扫描增强速度估算，但朗读来源继续使用 native，不被 OCR 覆盖。
- Tesseract 7 必须显式请求 `blocks` 输出，再把 block / paragraph / line 坐标适配为统一 TextSegment。

## 3. 建立统一取消边界

状态：已完成；基础会话、跨档策略、文本来源切换和鼠标点句共用取消边界

新增一个集中式会话入口，负责：

```text
cancelSpeechSession(reason)
→ stop audio
→ clear playback lock
→ invalidate generation
→ cancel queued/prefetched work
→ cancel active provider request
→ clear reading-line feedback
```

以下事件必须调用它：

- 页内远跳与目标 chunk 已改变；
- 暂停滚动；
- 换 PDF；
- 希声关闭；
- 切换到不同朗读策略；
- 点句朗读选择了新的目标句子；
- Provider、voice 或速度策略发生不兼容变化；
- 页面文本来源从 native 切换为 OCR。

取消后允许极短淡出，但旧内容不能继续完整播放。

## 4. 将 TTS 推理移出 PDF 服务进程

状态：child process 隔离与硬取消已完成；OCR / TTS 资源仲裁留待后续

优先采用 child process，而不是把 ONNX 推理继续放在 `server.mjs`。

建议结构：

```text
renderer
→ local reader server
→ tts-runtime-client
→ child process: tts-worker
→ Kokoro / future local provider
```

### 进程协议

- 请求包含 `requestId`、文本、语言、voice、speed 和 generation。
- 子进程单并发合成，队列有上限。
- renderer 取消后，server 向子进程发送 cancel。
- 如果底层推理不能协作取消，在短暂宽限后终止子进程并按需重建；不能只丢弃结果却继续吃 CPU。
- 模型加载失败后清除 rejected promise，允许显式重试。
- Provider 请求、预热和模型下载分别设置超时。
- PDF Range、静态资源和 heartbeat 在 TTS 合成期间仍保持响应。

### 性能验收

- 合成运行时自动滚动不出现持续掉帧或鼠标转圈。
- 取消后旧推理在可测时间内停止占用 CPU。
- PDF 本地服务的 heartbeat 不被一次合成长时间阻塞。
- 同一时间只允许一个重型 TTS 推理；OCR 与 TTS 的资源竞争有明确仲裁。

## 5. 重构六档朗读策略

状态：已完成；六档策略、触发、取消、过滤、预取与真实样本验收均已闭环

把当前 `off / whisper / flow` 与零散 tier 判断收敛为：

```text
master state: off / on
derived policy: snow-mist / aesthetic-walk / long-day / winding-stream / point-read
```

每个档位由一个纯策略对象描述：

- 是否自动朗读；
- 是否显示阅读线；
- 触发事件；
- 内容过滤规则；
- 目标语速；
- 是否预生成；
- 每次播放范围；
- 取消和去重规则。

已完成的共用骨架：

- `off / whisper / flow` 已从运行状态中移除，旧偏好只迁移为 `ttsEnabled` 布尔总开关。
- 六档策略由唯一的纯策略表派生，`H` 只切总开关，跨档只取消会话、不关闭总开关。
- 中、日文无空格句界和英文缩写、小数边界已抽为纯函数并有回归测试。
- 阅读线只在已开启的长日留痕显示；其他档位仍保留内部 reading Y。

### 雪国朦胧

- 已完成：舒适速度连续随读、过滤页边噪声，并将当前页用于选句、下一页用于预生成。
- 已完成：普通相邻向前翻页保留预生成；倒退、远跳、同页大幅跳读和文字来源变化会取消或作废旧结果。
- 40 页自动压力链已通过；独立源码服务上的真实 Kokoro / OCR 长跑也已通过，无需关闭用户正在运行的旧构建。
- 扫描 PDF 在雪国朦胧播放期间沿阅读方向补扫下一页；已识别、失败、暂停或其他档位不会重复触发。
- 连续随读。
- 舒适语速。
- 当前段播放时预生成下一段。
- 阅读线只作为内部同步依据，视觉隐藏。
- 永不朗读页眉页脚。
- 不做逐词视觉高亮。

### 美学散步

- 已完成：过滤脚注、旁注、页边噪声和完整配对括号，并按当前阅读线、页面高度与物理滚动速度计算视觉阅读窗口。
- 已完成：在 1.5–2.5 倍之间动态跟速；全文来不及则缩至完整首句，首句也来不及则安静跳过。
- 已完成：预生成保存下一段自己的文本、速度和截止时刻；同档调速导致计划变化时作废旧预生成，合成晚到也不播放。
- 连续自动朗读。
- 阅读线只作为内部同步依据，视觉隐藏。
- 语速在 1.5–2.5 倍之间动态选择，目标是跟上页面流动。
- 跳过页眉页脚、脚注、注释和成对括号内的内容。
- 若预测音频无法在视觉阅读窗口内完成，缩短到句子边界或安静跳过。
- 不允许为了追赶而重叠播放。

### 长日留痕

- 已完成：自动 tick 保持安静，阅读线和圆润乐符按钮只在本档显示，`R` 与按钮共用 action。
- 已完成：严格选择“句子起点位于阅读线下方”的最近一句，可重复点读，读完即停。
- 不自动朗读。
- 显示阅读线聚焦视点和乐符式触发按钮。
- `R` 与点击乐符走同一个动作。
- 触发后选择阅读线下方最近范围内的一句话，只播放一次，播放结束后停止。
- 没有触发就保持安静。
- 没有可读句时给出轻量反馈，不启动无意义合成。

### 流觞曲水

- 基础实现已完成：段落识别、段首预生成、段尾 ready-only 播放、迟到跳过、跨页滑动窗口和来源失效均已接线。
- 阅读线视觉隐藏，但内部继续计算真实阅读线位置。
- 只朗读自然段段首句。
- 阅读线越过当前段段首时，预生成下一自然段段首句。
- 阅读线越过当前段段尾时，播放已经准备好的下一段段首句。
- 预生成未完成时不阻塞滚动；完成得太晚则跳过。
- 同一自然段段首只播放一次。
- 原生文字和 OCR 都保留段落身份；原生文本用保守版式启发式推断，跨页只在同来源、正文、页边和缩进条件同时满足时合并。
- 普通相邻前向翻页保留已经准备好的段首；倒退、远跳、暂停、换文档或有效文字来源变化会立即作废。

### 强风吹拂与万物繁盛

- 已完成：两个档位不自动朗读、视觉不显示阅读线，只在顶部显示“点击以朗读”总开关，底部不重复显示希声控制。
- 已完成：native 与 OCR 坐标生成可命中的完整句子；支持跨行多片高亮、双栏小半径命中和旋转页面 viewport 坐标。
- 已完成：短按主鼠标键朗读，拖选、触摸、修饰键、跨句按下/抬起和超过 6 px 的移动均不触发。
- 已完成：点句以独立 scroll hold 暂停自动滚动；自然结束恢复点击前速度，用户滚动、调速、暂停、换页、切档、换来源或隐藏页面后不恢复。
- 已完成：单调 request token 保证最后点击优先；新点击经过统一取消入口停止旧音频、解码、scheduler 和实际 child 推理。
- 不自动朗读，阅读线视觉隐藏。
- 只在这两个档位显示“点句朗读”开关；它就是希声在最高两档的可见开关，不再叠加一层底部总开关。位置在页面顶部阅读时间估算的左侧。
- 开关开启后，鼠标悬停提示最近的完整句子。单击句子发起朗读，拖动仍可选择文字。
- 点句时暂停自动滚动，以淡纸黄或墨绿色短暂高亮整句；自然读完后恢复点击前的速度。
- 用户在朗读期间滚动、调速、暂停或切换页面时，不自动恢复滚动。
- 新点击立即取代旧点击，并经过统一取消入口停止旧音频、队列和实际推理。
- native text 可用时直接使用文字坐标；扫描页等待附近 OCR 提供句子坐标。文本未就绪时显示轻量状态，不合成空内容。
- `R` 仍只属于长日留痕，不触发鼠标点句模式。

## 6. 校正 UI 与快捷键

状态：已完成当前六档控制；总开关、档位提示、长日乐符、最高两档顶部点句、`H`/`R` 和阅读线显隐均已接线

- 保留一个清楚的希声总开关，具体行为由速度档位决定。
- 当前档位的朗读方式用一句短文案说明，不让用户再理解互相重叠的“低声 / 随读”状态。
- 长日留痕出现乐符按钮；其他档位不显示无效的手动打击标记。
- 强风吹拂和万物繁盛在顶部阅读时间估算左侧显示“点句朗读”开关；其他四档不显示。
- `R` 只在长日留痕有效，并与乐符按钮调用同一个公开 action，不直接操作 controller 内部状态。
- 模型首次使用前仍需明确说明本地下载；本轮只保留入口和状态，不重做模型选择。
- 系统设置减少动画时，乐符反馈减少位移，但可读性和键盘操作保留。

顶部点句入口已和句子命中、暂停/恢复、高亮及最后点击优先一起交付；纯触屏设备因本轮不支持触摸点句而隐藏该入口。

## 7. 集成验收

状态：已完成；自动化、固定多语样本和大型扫描书真实连续朗读均已通过

### 自动化

- 单元测试覆盖六档纯策略。
- 集成测试覆盖 OCR debounce、文本来源切换、页内跳转、硬取消、R 门槛、预生成时序、点句命中与滚动恢复。
- server 测试覆盖 child process 正常响应、超时、崩溃、重启和取消。
- 原有 Range、渲染、缓存、阅读速度和存储测试不得回退。

### 真实样本

1. 原生英文单栏书：直接开放希声，不启动 OCR。已完成。
2. 英文双栏论文：已完成。窄中缝不再把左右栏同一高度的文字拼成一句；点句仍按左栏后右栏排序。
3. 中文扫描书：已完成。第 50 页自动形成 10 个 OCR chunks、13 个完整点句目标并开放朗读；模型误字作为当前 PoC 前的已知限制保留。
4. 日文短文：已完成。第 80 页连续正文为 `ocr-ready`，中日混合语言路由与按行点句可用；第 50 页词汇表为 `ocr-poor`，只参与速度估算、不开放朗读。声音质量不在本轮验收。
5. 584 页、约 262 MB 扫描书：已验证自动排队、运行、OCR chunks、切源、流觞曲水跨页预生成，以及雪国朦胧真实 Kokoro 连续跨页、远跳硬取消和前向 OCR 补扫。

### 阶段出口

当前 `GOAL.md` 的八项完成线已由主线程最终 reviewer 对照代码、273 项自动测试和真实大型扫描书证据复核通过。下一阶段可以进入：

1. 中文、英文、日文模型 PoC；
2. 依赖风险、许可证、版本号和安装包发布工程。
