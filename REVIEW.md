# 公开验证摘要

本文汇总公开版本的自动化、人工验证范围与已知限制。

## v4.0.0 验证结果

发布流水线完成：

- Node 回归测试通过
- CJK worker Python 单元测试通过
- 英文离线模型冒烟测试通过
- macOS 与 Windows 桌面包构建通过
- 两个平台的打包后运行时冒烟测试通过
- Release 同时生成 macOS zip、Windows portable exe 与 `SHA256SUMS.txt`

## 主要覆盖范围

- HTTP Range、File transport 与大文件分段读取
- 页面布局、渲染优先级、取消和缓存逐出
- 原生文字、OCR 质量判断与来源切换
- 中文、英文和日文文本切分与语言路由
- 六档朗读策略、预取、点句和播放锁
- 跳页、暂停、停止、超时和 worker 崩溃后的取消与恢复
- 本地服务在 TTS 推理期间的响应性
- 打包后模型、worker、许可和静态资源完整性

## 人工验证范围

发布前使用多种文字型、双栏和扫描 PDF 检查：

- 原生文字优先路径
- 扫描页 OCR fallback
- 跨页滚动与朗读连续性
- 中英、日英混排
- 桌面与窄窗口布局
- 跳页和用户操作后的旧任务取消

测试材料本身不进入公开仓库。

## 保留限制

- OCR 置信度不等于逐字准确率。
- 多语言声音质量仍受模型与文本前端限制。
- 完整应用级 DOM/E2E 编排测试仍可继续补充。
- 未签名桌面包可能触发系统安全提示。

可复现的版本事实以 GitHub Actions、[`CHANGELOG.md`](./CHANGELOG.md) 和 Release 页面为准。
