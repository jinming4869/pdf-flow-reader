# 当前工作

## v4「希声」工程整理

- [x] 创建独立分支 `feature/v4-hisheng`。
- [x] 将 Phase 1 原型保存为检查点 `4647acd`。
- [x] 恢复 `PLAN.md`，并新增 `GOAL.md`、`CONTEXT.md` 和 v4 文档索引。
- [x] 将旧 PRD、模型草案、工程日志和竞品调研按用途分层。
- [x] 完成第二次项目管理文档整理并通过 reviewer 复核。

## v4 基础修复

- [x] 先补 OCR debounce 饥饿的失败测试。
- [x] 修复 pending OCR 任务被状态刷新重复推迟。
- [x] 建立 native / OCR 页面文本状态机。
- [x] native chunks 足够时直接开放希声。
- [x] native 文本不足时自动建议或调度附近页 OCR。
- [x] 修复 Tesseract 7 默认只返回 text、没有旧式 lines/words，导致 OCR chunks 永远为空的问题。
- [x] 保留 native-ready 页的手动扫描增强；只校准速度估算，不覆盖 native 朗读文本。
- [x] 修复 CLI 未传 `--idle-ms` 时误删 PDF 首个位置参数、启动器无法带文件启动的问题。
- [x] 为页内跳转、暂停、换 PDF 和关闭希声建立统一取消入口。
- [x] 六档策略切换与新点句接入同一个取消入口。
- [x] 修复页内跳转后旧音频、异步解码和旧合成继续的问题。
- [x] 将 Kokoro 推理移到 child process。
- [x] 为活动推理实现硬取消、超时、崩溃恢复、队列上限和单并发。
- [x] 让 PDF Range 与 heartbeat 在合成期间保持响应。
- [x] 让 `R` 经过统一可用性和预热入口。
- [x] 修复 CJK 首句切分，并保护闭引号、英文缩写与小数。

## v4 六档朗读逻辑

- [x] 把当前 `off / whisper / flow` 重构为布尔总开关 + 六档纯策略。
- [x] 雪国朦胧跨页实现：当前页只参与选句，下一页进入预取池；普通向前翻页复用，跳读和来源变化作废。
- [x] 不关闭用户正在运行的旧应用，改用隔离源码服务与无头浏览器，在 584 页扫描 PDF 上完成真实 Kokoro 跨页长跑。
- [x] 修复扫描 PDF 进入已识别页面后 OCR 前向窗口耗尽；雪国朦胧播放时持续补扫下一页。
- [x] 美学散步：按视觉阅读窗口在 1.5–2.5 倍动态跟速；来不及则缩至完整首句或安静跳过，旧预生成和晚到结果不会追赶播放。
- [x] 长日留痕：只显示阅读线和乐符按钮，`R` 或点击后严格读下方最近一句，读完即停。
- [x] 流觞曲水：隐藏阅读线视觉，越过段首时预生成下一段段首，越过段尾时只播放已经 ready 的结果；迟到、繁忙和失效结果安静跳过。
- [x] 强风吹拂、万物繁盛：仅提供顶部“点击以朗读”，鼠标点哪里读哪里。
- [x] 点句时暂停滚动并柔和高亮整句；自然读完后恢复，用户中途操作则不恢复。
- [x] 新点句取消旧音频与实际推理，native text 和 OCR 坐标都能命中完整句子。
- [x] 六档纯策略、前三档和流觞曲水的内容过滤、触发、去重与滑动窗口测试。
- [x] 最高两档点句命中、暂停/恢复、最后点击优先和取消时序测试。
- [x] 流觞曲水阶段通过独立 reviewer 复核，并在 584 页扫描书上完成第 11–13 页连续运行目测。
- [x] 使用五类真实样本完成固定基线验收：原生英文单栏、英文双栏、中文扫描正文、日文连续正文 / 词汇表反例和大型扫描书。
- [x] 修复窄中缝英文论文跨栏串句，并为 readable chunks 与点句目标补永久回归。
- [x] 将中日文 OCR 点句改用稳定 line bbox，英文 OCR 保留 word bbox；去除扫描边框误识别的行首尾竖线。
- [x] 将 OCR 的“可用于速度密度估算”与“可用于朗读”分开；日文词汇表只校准估算，不开放朗读。
- [x] 为日文文件名与识别文本加入专用语言路由，混合教材使用 `jpn+chi_sim+eng`。
- [x] 最高两档点句阶段经独立 reviewer 三轮复核，最终无 P0/P1/P2。

## 暂缓

- [ ] 下一阶段做中文、英文、日文模型 PoC。
- [ ] 模型 PoC 通过后，再处理依赖审计、许可证、版本号、签名和发布包。

## 第二步

- [x] 重构速度控件，加入六档名称、px/s 与估算字/词每分钟。
- [x] 接入 PDF.js 文本层统计和附近五页缓存。
- [x] 实现手动调速粒子和自动密度 morph，支持减少动画。
- [x] 打包 OCR 引擎、WASM 和四语模型，接入边缘按钮、队列、进度与会话缓存。
- [x] 修正数字触发“字/分”的语言误判，并为英文数字表格补永久回归测试。
- [x] 把 OCR 入口改为“扫描页增强估算”，补充适用场景、用途和本地隐私说明。
- [x] 更新 README、第三方许可和安装体积说明。
- [x] 用真实英文 PDF 完成桌面与 319 px 窄屏目测，验证英文页只显示“词/分”、扫描增强说明和状态布局。

## 第三步

- [x] 与用户确认长文档规模、首屏目标、纸页底稿、自动缓存、最终画质、OCR 跳页策略和平台优先级。
- [x] 确认真实主样本：584 页、约 262 MB 的本地 PDF。
- [x] 实现 File 分段读取和完整 HTTP Range/HEAD 语义。
- [x] 建立增量页面 shell、布局索引和阅读锚点保持。
- [x] 实现页面渲染调度、取消、动态缓存、像素预算和 canvas 逐出。
- [x] 完成纸页底稿、首次“正在显影第一页”和高清淡入。
- [x] 让 OCR 与页面调度协同，保留文字缓存并避免连续跳页重建 worker。
- [x] 增加只读性能诊断，以及 Range、调度、缓存、布局和 File transport 永久测试。
- [x] 使用 584 页、约 262 MB 主样本完成首屏、跳页、回看、OCR、清晰度和内存趋势验收。
- [x] 独立 reviewer 复核第三步实现与证据。

## 后续加固（不阻塞第三步）

- [ ] 将“两次异步 source factory、后选文件先完成”的 latest-selection 行为抽成可注入的永久竞态测试。
