# v5.2「回望」风险登记册

状态：冻结（2026-08-16）

| ID | 风险 | 等级 | 最晚验证阶段 | 缓解与退出证据 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V2-R1 | Zotero local API 版本差异（7/8 只读、10 写入授权）导致写入路径失效 | 高 | S2（Zotero PoC） | 版本探测；Web API 主路径 + local API 只读匹配；四种安静失败路径实测 | 开放 |
| V2-R2 | Zotero Web API key 权限不足或库 id 误配导致写错库 | 高 | S2 | key 权限校验（/keys/current）；library id 从 local API 读取；写入前回显目标条目 | 开放 |
| V2-R3 | 整书正文发送泄漏私人内容（隐私放大） | 高 | S3（回望） | 逐项授权清单；分块最小化；脱敏样本测试；诊断无内容 | 开放 |
| V2-R4 | 全文总结长书超时或 token 超限 | 高 | S3 | 分块 + 渐进式生成；可取消；失败保留已完成部分 | 开放 |
| V2-R5 | AI 分析输出覆盖用户情绪或痕迹 | 高 | S3 | AI 输出与用户事实分列存储；trace id 关联只读；不写入 reading-trace | 开放 |
| V2-R6 | 分析 Prompt 复杂化导致输出不可读或格式漂移 | 中 | S4 | 结构化输出契约；长度与字段校验；失败降级为纯文字摘要 | 开放 |
| V2-R7 | LSE 模板与官方最新版不一致 | 低 | S4 | 采用公开模板字段并在界面标注版本与来源 | 开放 |
| V2-R8 | 强分析凭据与快速回声凭据混淆 | 中 | S1 | 槽位分离（echo-fast / echo-strong）；配置界面分区显示 | 开放 |
| V2-R9 | Zotero 导入上传 PDF 附件失败或产生重复条目 | 高 | S2 | 上传前附件去重检查；创建失败不写 note；用户可见状态 | 开放 |
| V2-R10 | macOS PDF 文件关联注册失败（未签名包限制） | 中 | S2 | packaged 实测“打开方式”；失败降级为“导入 Zotero”单向可用 | 开放 |

## 处理规则

- 高风险项未在对应阶段关闭，不得进入下一阶段。
- 风险关闭必须在 REVIEW 附测试、测量或真实操作证据。
- 缓解方案若改变产品行为，必须重新取得产品所有者决定。
